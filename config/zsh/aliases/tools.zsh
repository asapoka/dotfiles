#!/usr/bin/env zsh

# =============================================================================
# モダンツールエイリアス
# =============================================================================
# 従来のUnixコマンドをモダンな代替ツールに置き換える

# lsをlsd（colorful ls）に置き換え
alias ls='lsd'
alias ll='ls -l'        # 詳細表示
alias la='ls -a'        # 隠しファイルも表示
alias lt='ls -t'        # 更新時刻順でソート

# grepをripgrep（高速検索）に置き換え
alias grep='rg'

# catをbat（シンタックスハイライト付き）に置き換え
alias cat='bat'