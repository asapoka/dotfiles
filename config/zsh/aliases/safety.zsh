#!/usr/bin/env zsh

# =============================================================================
# 安全性向上エイリアス
# =============================================================================
# ファイル操作時の事故防止のためのエイリアス

# ファイル削除時に確認を求める
alias rm='rm -i'

# ファイル移動時に上書き確認を求める
alias mv='mv -i'

# ファイルコピー時に上書き確認を求める
alias cp='cp -i'

# ディレクトリ作成時に親ディレクトリも自動作成
alias mkdir='mkdir -p'