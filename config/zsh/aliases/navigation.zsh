#!/usr/bin/env zsh

# =============================================================================
# ディレクトリ移動エイリアス
# =============================================================================
# 上位階層への移動を簡単にするエイリアス

# 1つ上の階層へ移動
alias ..="cd .."

# 2つ上の階層へ移動
alias ...="cd ../.."

# 3つ上の階層へ移動
alias ....="cd ../../.."

# 4つ上の階層へ移動
alias .....="cd ../../../.."