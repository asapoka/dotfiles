#!/usr/bin/env zsh

# =============================================================================
# Starship初期化
# =============================================================================
# Starshipプロンプトの初期化設定

# Starshipが利用可能な場合のみ初期化
if command -v starship >/dev/null 2>&1; then
  eval "$(starship init zsh)"
fi